# Cloud Foundry Deployment Reference — TypeScript (Express)

This reference covers deploying the Express-based TypeScript LangGraph A2A agent to SAP BTP Cloud Foundry using the Node.js buildpack.

## Table of Contents

1. [Project Structure](#1-project-structure)
2. [package.json](#2-packagejson)
3. [manifest.yml](#3-manifestyml)
4. [Procfile](#4-procfile)
5. [.env.example](#5-envexample)
6. [Deployment Steps](#6-deployment-steps)
7. [BTP Destination Configuration](#7-btp-destination-configuration)
8. [Troubleshooting](#8-troubleshooting)

---

## 1. Project Structure

```
<agent-name>/
├── src/
│   ├── index.ts              # Express + A2A server entry point
│   ├── agent.ts              # LangGraph.js ReAct agent
│   ├── executor.ts           # A2A protocol bridge
│   ├── agentCard.ts          # Agent Card definition
│   ├── tools.ts              # Agent's tools (Zod schemas)
│   └── llm.ts                # LLM provider configuration
├── joule-capability/
│   ├── capability.sapdas.yaml
│   ├── capability_context.yaml
│   ├── da.sapdas.yaml
│   ├── functions/
│   │   └── call_agent.yaml
│   └── scenarios/
│       └── invoke_agent.yaml
├── dist/                     # Compiled JS (generated by tsc)
├── package.json
├── tsconfig.json
├── manifest.yml              # CF deployment manifest
├── Procfile
├── .env.example
└── README.md
```

---

## 2. package.json

```json
{
  "name": "my-a2a-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@a2a-js/sdk": "0.3.10",
    "@langchain/core": "^1.1.32",
    "@langchain/langgraph": "^1.2.2",
    "@sap-ai-sdk/langchain": "^2.8.0",
    "@sap-ai-sdk/orchestration": "^2.8.0",
    "express": "^4.21.0",
    "uuid": "^10.0.0",
    "zod": "^3.25.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
```

---

## 3. manifest.yml

```yaml
---
applications:
  - name: my-a2a-agent
    memory: 512M
    disk_quota: 1G
    instances: 1
    buildpacks:
      - nodejs_buildpack
    command: npm start
    env:
      MODEL_NAME: gpt-4.1
      NODE_ENV: production
    services:
      - aicore  # TODO: Replace with your AI Core service instance name (run 'cf services' to find it)
    routes:
      - route: my-a2a-agent.cfapps.<landscape>.hana.ondemand.com
```

---

## 4. Procfile

```
web: npm start
```

---

## 5. .env.example

```bash
# SAP GenAI Hub model (must be deployed on your AI Core instance)
MODEL_NAME=gpt-4.1

# Server
PORT=8080

# For local development — SAP GenAI Hub credentials:
# AICORE_SERVICE_KEY={"serviceurls":{"AI_API_URL":"https://..."},"clientid":"...","clientsecret":"...","url":"https://..."}
```

---

## 6. Deployment Steps

### Prerequisites

1. **CF CLI**: `cf version`
2. **Node.js 20+**: `node --version`
3. **Logged in**: `cf login -a <api-endpoint>`

### Build & Deploy

**Important:** You must build TypeScript locally before pushing, because `nodejs_buildpack` with `NODE_ENV=production` skips devDependencies (which includes `typescript`). The compiled `dist/` directory must be included in the upload.

```bash
# 1. Install dependencies and build locally
npm install
npm run build

# 2. Push to Cloud Foundry (dist/ is uploaded with the app)
cf push

# 3. Verify
cf apps
cf logs my-a2a-agent --recent

# 4. Test agent card
curl https://my-a2a-agent.cfapps.<landscape>.hana.ondemand.com/.well-known/agent.json
```

### .cfignore

```
node_modules/
.env
src/
```

Only `dist/`, `package.json`, `tsconfig.json`, `manifest.yml`, and `Procfile` need to be uploaded. Source files in `src/` are not needed at runtime.

---

## 7. BTP Destination Configuration

### Standard (NoAuthentication)

| Property | Value |
|----------|-------|
| Name | `MyAgent_A2A` |
| Type | HTTP |
| URL | `https://my-a2a-agent.cfapps.<landscape>.hana.ondemand.com` |
| Proxy Type | Internet |
| Authentication | NoAuthentication |

Additional property: `HTML5.DynamicDestination` = `true`

### With Principal Propagation (OAuth2UserTokenExchange)

If the agent uses `--with-principal-propagation`, the Joule-facing destination must use `OAuth2UserTokenExchange` instead of `NoAuthentication`. See `skills/principal-propagation/references/pp-btp-config.md` for the full destination configuration.

Also requires two additional service bindings in `manifest.yml`:
- `<agent-name>-xsuaa` — XSUAA instance created from `xs-security.json`
- `<agent-name>-destination` — Destination service instance

And a second destination for the CF agent → S/4HANA leg (`OAuth2SAMLBearerAssertion`). See the PP reference for all details.

---

## 8. Troubleshooting

### Build fails on CF

- Check that `tsconfig.json` and `src/` are included in the upload
- Verify `"build": "tsc"` in package.json scripts
- Check Node.js version: add `"engines": {"node": ">=20.0.0"}` to package.json

### Module resolution errors

- Use `"type": "module"` in package.json for ESM
- Set `"module": "NodeNext"` and `"moduleResolution": "NodeNext"` in tsconfig.json

### @a2a-js/sdk import issues

- The SDK uses subpath exports. Import from `@a2a-js/sdk/server` for server-side classes
- Types come from `@a2a-js/sdk` directly

### Memory issues

- Node.js typically needs 256M-512M. If you hit heap limits: add `NODE_OPTIONS=--max-old-space-size=512` to env
